{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"],
    // Pin resolution to this repo's workspace sources (avoids stale types when node_modules links point elsewhere)
    "baseUrl": ".",
    "paths": {
      "@fractal-office/pptx-engine": ["../pptx-engine/src/index.ts"],
      "@fractal-office/pptx-engine/table-grid": ["../pptx-engine/src/table-grid.ts"]
    }
  },
  "include": ["src", "tests"]
}
